# UX-01 Foundations

- [ ] T001 Add semantic design tokens V2
  - **Milestone:** M001
  - **Epic:** E001
  - **Risk:** medium
  - **Implementation files:** `frontend/src/styles.scss`
  - **Test files:** none
  - **Validation commands:** npm --prefix frontend run format:check; git --no-pager diff --check
  - **Final PR review required:** yes
  - **Goal:** Introduce the semantic visual foundation required by the Protolume UX redesign without changing the public page structure.
  - **Dependencies:** None
  - **Acceptance criteria:** Semantic tokens exist for canvas, surfaces, text, borders, accent, statuses, motion durations, easing, radii and content widths. Existing CSS variables remain available as compatibility aliases. The task does not globally redesign existing pages.
  - **Test requirements:** Existing frontend formatting and stylesheet compilation contracts must remain valid.
  - **Parallelizable:** no
  - **Notes:** Do not remove existing CSS variables and do not switch the whole application to the final dark theme in this task.