stary manager loop został usunięty,
nie wolno go odtwarzać na podstawie historii,
Spec Kit opisuje wymagania, ale nie jest runtime state,
commit, push, merge i deployment pozostają wyłączone do czasu implementacji kontrolera,
w danym momencie może pisać tylko jeden wykonawca.

Docelowy AGENTS.md zostanie wygenerowany później jako dokument nowej architektury.